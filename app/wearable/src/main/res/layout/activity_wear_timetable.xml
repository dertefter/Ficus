<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"

    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:id="@+id/timeTableScrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:paddingStart="8dp"
    android:paddingEnd="8dp">
    <requestFocus
        android:focusable="true"
        android:focusableInTouchMode="true"/>

    <LinearLayout

        android:id="@+id/timeTableLinearLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="@drawable/empty_divider"
        android:orientation="vertical"
        android:paddingTop="62dp"
        android:paddingBottom="58dp"
        android:showDividers="middle">

        <ProgressBar
            android:id="@+id/loadingTimeTable"
            style="?android:attr/progressBarStyle"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_gravity="center"
            android:indeterminateTint="@color/md_theme_dark_onSurface"
            android:progressTint="@color/md_theme_dark_onSurface" />

    </LinearLayout>

</ScrollView>